# 复制为 .env 后修改。本地测试可用简单密码，上 VPS 务必改为强密码。

# Miniflux 管理员（与 Tidyflux 登录用同一账号）
MINIFLUX_USERNAME=admin
MINIFLUX_PASSWORD=your_secure_password

# PostgreSQL（仅内部使用，与上面可不同）
POSTGRES_PASSWORD=secret

# 可选：端口（默认 8080 / 8812）
# MINIFLUX_PORT=8080
# TIDYFLUX_PORT=8812

# 时区（简报等用）
TZ=Asia/Shanghai

# 抓取间隔（分钟）
POLLING_FREQUENCY=60
